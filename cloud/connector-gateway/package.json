{
  "name": "@gilbert-codex/connector-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Firebase-authenticated Cloud Run gateway for hosted GitHub, Google, and Discord connectors.",
  "scripts": {
    "check": "node --check src/firebase.js && node --check src/http.js && node --check src/server.js",
    "start": "node src/server.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0"
  }
}
